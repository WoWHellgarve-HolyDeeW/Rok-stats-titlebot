# ============================================
# BACKEND CONFIGURATION
# ============================================
# Copy this file to .env and adjust values as needed
# cp .env.example .env

# Database URL
# SQLite (default - good for single server):
DATABASE_URL=sqlite:///./rokstats.db

# PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:password@localhost:5432/rokstats

# Secret key for JWT authentication (CHANGE THIS!)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-random-secret-key-at-least-32-chars

# Token for scanner data ingestion (CHANGE THIS!)
# This is used by the scanner to upload data to the API
INGEST_TOKEN=change-me-to-a-random-token

# Redis for async processing (optional)
# Uncomment if you want background job processing
# REDIS_URL=redis://localhost:6379
# USE_ASYNC_INGEST=1

# Server settings
HOST=0.0.0.0
PORT=8000
